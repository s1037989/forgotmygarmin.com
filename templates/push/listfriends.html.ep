% layout 'landing';
% if ( flash 'message' ) {
<div class="message"><%= flash 'message' %></div>
% }
<h1>Select an athlete to push an activity to</h1>
% foreach ( @$athletes ) {
  <%= link_to $_->{firstname} => url_for 'push_listactivities', destination => $_->{id} %><br />
% }
